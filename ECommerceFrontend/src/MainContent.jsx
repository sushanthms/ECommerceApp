import "./MainContent.css"
function MainContent(){
    return(
<main>
          <section>
            <h2>Welcome to the WebPage</h2>

            <p>I am learning HTML, CSS and JavaScript by building a webpage step by step.Yes 👍 That is the better order for your case:

And because the `768px` rules come last, they can override the `1024px` rules when the screen becomes **768px or smaller**.
</p>
          </section>
        </main>

    )
}

export default MainContent;